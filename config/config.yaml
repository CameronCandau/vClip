# OpIndex configuration file
# Named workspaces let you switch source sets without changing config paths.

default_workspace: default

workspaces:
  default:
    files: []
    directories:
      - "~/Documents/commands"
      - "~/.local/share/opindex"
    recursive: true
    file_patterns:
      - "*.md"
      - "*.markdown"

# Rofi interface configuration
rofi:
  # Maximum number of lines to show
  max_lines: 15

  # Prompt text
  prompt: "Commands"

  # Window width as percentage of screen (0-100)
  window_width: 60

  # Element height (lines per entry - use 2 for two-line format with command preview)
  element_height: 2

# Cache configuration
cache:
  # Enable caching for faster startup
  enabled: true

  # Cache directory (null means use default: ~/.cache/opindex)
  directory: null

# Copy commands exactly as written by default.
# Set this to true if you want uppercase variables like $HOST substituted before copy.
substitute_variables: false

# Variable definitions for command substitution
# Variables defined here are substituted silently — never prompted.
# Variables NOT listed here will trigger a rofi prompt when a command uses them,
# pre-filled from the last-used value in ~/.cache/opindex/vars.json.
variables:
  # LHOST: "10.10.14.5"
  # WORDLIST: "/usr/share/wordlists/rockyou.txt"
